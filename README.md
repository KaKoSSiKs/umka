# УМКА — сайт проекта

Одностраничный адаптивный сайт проекта «УМКА».

## Структура
- `index.html` — разметка и контент
- `style.css` — дизайн, тёмная тема, адаптивность
- `script.js` — мобильное меню и анимации появления
- `assets/` — логотип и фотографии из презентации проекта
- `Dockerfile`, `docker-compose.yml`, `nginx/` — контейнер nginx для VPS
- `DEPLOY.md` — размещение на сервере (умка.москва)

## Запуск локально
Откройте `index.html` в браузере.

Или через Docker:

```bash
docker compose up --build
```

Сайт будет на `http://localhost`.

## Размещение на VPS
Пошаговая инструкция (DNS, Docker, HTTPS): см. [DEPLOY.md](DEPLOY.md).
